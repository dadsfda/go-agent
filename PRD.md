# 智能招聘系统 PRD

## 1. 文档信息

- 产品名称：智能招聘系统
- 文档类型：产品需求文档
- 需求来源：[一、作业考核核心目标.md](./一、作业考核核心目标.md)
- 目标版本：大作业交付版
- 编写日期：2026-05-09

## 2. 背景与目标

本系统面向招聘场景，提供 HR 管理端与候选人用户端两套独立前端，并通过 Gin Web 网关服务、Logic gRPC 业务服务、MySQL、私有 OSS 与 Eino 框架完成完整业务闭环。

### 2.1 产品目标

1. 支持 HR 发布和管理本人岗位，并查看岗位投递候选人。
2. 支持候选人浏览公开岗位、维护结构化个人档案、上传合规简历并投递岗位。
3. 支持 HR 通过 AI 对话窗口查询真实业务数据，并持久化对话历史。
4. 通过私有 OSS 与签名 URL 完成简历安全上传、下载和访问。
5. 通过 Web 服务与 Logic 服务的两层 gRPC 架构体现服务分层。

### 2.2 成功标准

1. HR 与候选人账号体系、权限边界清晰，未授权用户无法访问受保护能力。
2. 候选人从浏览岗位到完善资料、上传简历、投递岗位的链路完整可演示。
3. HR 从登录到岗位管理、候选人台账查看、AI 业务问答的链路完整可演示。
4. 简历文件只进入私有 OSS，不在本地进行源文件缓存或兜底存储。
5. AI 问答必须基于 MySQL 真实业务数据，并通过 Eino 基础 Chat 组件完成。
6. 提交物符合指定目录结构、文档要求与视频录制要求。

## 3. 用户角色

### 3.1 游客

未注册或未登录用户。游客可以浏览全平台公开岗位列表，但不能投递岗位、编辑档案或上传简历。

### 3.2 候选人

已注册并登录的求职用户。候选人可以维护个人档案、上传简历、浏览岗位并投递岗位。

### 3.3 HR 管理员

已注册并登录的招聘管理用户。HR 可以管理本人创建的岗位，查看岗位投递候选人，并使用 AI 对话查询招聘业务数据。

## 4. 产品范围

### 4.1 本期必做

1. HR 管理端独立前端工程。
2. 候选人用户端独立前端工程。
3. Gin Web 网关服务。
4. Logic gRPC 核心业务服务。
5. MySQL 数据存储。
6. 私有 OSS 简历上传与签名访问。
7. Eino 基础 Chat AI 问答。
8. `api.md`、`db.md`、`README.md`、`answer.md` 等交付文档。

### 4.2 本期不做

1. 不考察 Docker。
2. 不做向量数据库。
3. 不做 RAG。
4. 不做简历智能匹配。
5. 简历内容不与 AI 交互。
6. 不允许裸写 HTTP 接口直连大模型替代 Eino。

### 4.3 可选能力

简历解析自动填充候选人结构化档案字段属于可选功能，不作为本期核心验收依赖。

## 5. 核心业务流程

### 5.1 游客浏览岗位流程

1. 游客进入候选人用户端。
2. 系统展示全平台公开岗位列表。
3. 游客可查看岗位基础信息。
4. 系统不展示岗位投递按钮。

### 5.2 候选人投递岗位流程

1. 候选人注册或登录账号。
2. 候选人编辑结构化个人档案。
3. 候选人上传 PDF、DOC 或 DOCX 格式简历。
4. 后端校验文件后缀与文件头。
5. 合规简历通过签名 URL 直传私有 OSS。
6. 候选人在岗位详情或列表中发起投递。
7. 后端校验候选人是否已完善档案、是否已上传合规简历。
8. 校验通过后创建岗位投递记录。
9. 校验失败时拦截投递，并提示补齐资料或上传简历。

### 5.3 HR 岗位管理流程

1. HR 注册或登录账号。
2. 后端签发 JWT 令牌。
3. HR 进入管理端。
4. HR 新增、编辑或下架本人创建的岗位。
5. 后端校验岗位归属，禁止 HR 操作他人岗位。

### 5.4 HR 候选人台账查看流程

1. HR 进入候选人台账页面。
2. 系统分页展示岗位对应的投递候选人。
3. HR 可查看候选人结构化档案。
4. HR 可查看候选人 OSS 云端简历记录。
5. 简历访问通过签名 URL 完成，不能依赖公开读权限。

### 5.5 HR AI 问答流程

1. HR 在管理端常驻 AI 对话窗口输入自然语言问题。
2. Web 服务接收请求并完成 JWT 鉴权和参数校验。
3. Web 服务通过 gRPC 调用 Logic 服务。
4. Logic 服务解析问题意图并查询 MySQL 真实业务数据。
5. Logic 服务拼接标准业务上下文与用户问题。
6. Logic 服务调用 Eino 基础 Chat 组件。
7. 系统返回自然语言统计回答。
8. HR 提问与 AI 回复成对写入 MySQL。
9. HR 重新进入页面时，系统加载历史对话上下文。

## 6. 功能需求

### 6.1 账号与登录

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| AUTH-001 | HR 注册/登录 | HR 使用账号密码登录管理端，后端签发 JWT | P0 |
| AUTH-002 | 候选人注册/登录 | 候选人使用账号密码登录用户端，后端签发 JWT | P0 |
| AUTH-003 | JWT 鉴权 | 受保护接口必须校验 JWT，无令牌禁止访问 | P0 |
| AUTH-004 | 双角色隔离 | HR 与候选人权限隔离，不能跨角色访问接口能力 | P0 |

### 6.2 候选人用户端

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| USER-001 | 公开岗位浏览 | 游客、候选人均可浏览公开岗位列表 | P0 |
| USER-002 | 投递按钮展示控制 | 仅登录候选人展示投递按钮 | P0 |
| USER-003 | 个人档案编辑 | 支持填写姓名、联系电话、最高学历、毕业院校、工作/项目经历、核心技能标签 | P0 |
| USER-004 | 档案实时保存 | 候选人档案写入 MySQL，支持后续编辑和同步更新 | P0 |
| USER-005 | 简历上传 | 支持上传 PDF、DOC、DOCX 简历 | P0 |
| USER-006 | 简历格式校验 | 后端校验文件后缀与文件头，拦截非法格式 | P0 |
| USER-007 | OSS 直传 | 合规简历直传私有 OSS，本地不缓存源文件 | P0 |
| USER-008 | 岗位投递 | 候选人完成档案和简历后可一键投递岗位 | P0 |
| USER-009 | 投递拦截提示 | 未完善档案或未上传合规简历时拦截投递并提示补齐 | P0 |
| USER-010 | 简历解析自动填充 | 解析候选人上传的简历内容，自动填充结构化个人档案字段；该能力为可选功能，不作为本期核心验收依赖 | P2 |

### 6.3 HR 管理端

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| HR-001 | 岗位新增 | HR 可新增招聘岗位 | P0 |
| HR-002 | 岗位编辑 | HR 可编辑本人创建的岗位 | P0 |
| HR-003 | 岗位下架 | HR 可下架本人创建的岗位 | P0 |
| HR-004 | 岗位权限校验 | HR 不能编辑或下架他人创建的岗位 | P0 |
| HR-005 | 候选人台账 | 分页查看岗位对应投递候选人 | P0 |
| HR-006 | 档案查看 | 查看候选人结构化个人档案 | P0 |
| HR-007 | 简历记录查看 | 查看 OSS 云端简历记录，并通过签名 URL 访问 | P0 |
| HR-008 | AI 对话窗口 | 管理端内置常驻 AI 对话窗口 | P0 |
| HR-009 | AI 历史加载 | 页面刷新或重新进入时加载历史对话上下文 | P0 |

### 6.4 AI 业务问答

| 编号 | 功能 | 说明 | 优先级 |
| --- | --- | --- | --- |
| AI-001 | 自然语言提问 | HR 可输入自然语言业务问题 | P0 |
| AI-002 | 意图解析 | 后端解析问题意图，映射到业务数据查询 | P0 |
| AI-003 | 真实数据查询 | 查询 MySQL 中的岗位、投递、候选人等真实数据 | P0 |
| AI-004 | Eino 调用 | 使用 Eino 基础 Chat 组件生成回答 | P0 |
| AI-005 | 对话持久化 | HR 提问与 AI 回复成对存入 MySQL | P0 |
| AI-006 | 统计类问题支持 | 支持投递总人数、单岗位投递统计、候选人筛选、岗位热度等问题 | P0 |

## 7. 权限规则

1. 游客只能访问公开岗位浏览能力。
2. 未登录用户不能投递岗位。
3. 候选人只能维护自己的档案、简历和投递行为。
4. HR 只能管理本人创建的岗位。
5. HR 可查看岗位对应的投递候选人和简历记录。
6. 所有后台管理能力必须经过 JWT 校验。
7. Web 服务负责统一鉴权入口，Logic 服务仍需承载核心权限管控逻辑。

## 8. 数据需求

### 8.1 核心数据对象

1. 用户账号：账号 ID、角色、账号名、密码摘要、创建时间、更新时间。
2. HR 岗位：岗位 ID、创建 HR ID、岗位名称、岗位描述、岗位状态、创建时间、更新时间。
3. 候选人档案：候选人 ID、姓名、联系电话、最高学历、毕业院校、工作/项目经历、核心技能标签、创建时间、更新时间。
4. 简历记录：简历 ID、候选人 ID、OSS 对象 Key、文件名、文件格式、上传时间。
5. 投递记录：投递 ID、岗位 ID、候选人 ID、简历 ID、投递时间。
6. AI 对话历史：消息 ID、HR ID、提问内容、回复内容、创建时间。

### 8.2 数据存储要求

1. MySQL 统一存储账号、岗位、候选人档案、投递关联、简历元信息与 AI 对话历史。
2. 私有 OSS 仅存储合规简历源文件。
3. 本地服务不得缓存或留存简历源文件。

## 9. 技术与架构约束

### 9.1 服务分层

1. Web 服务使用 Gin 实现，仅负责 HTTP 接入、跨域、JWT 鉴权、参数校验。
2. Logic 服务通过 gRPC 对外提供核心业务能力。
3. Web 服务与 Logic 服务之间只能通过 gRPC 通信。
4. 禁止在 Web 服务中编写核心业务逻辑。
5. 禁止同工程本地函数直连调用 Logic 能力。

### 9.2 OSS 安全

1. OSS Bucket 必须为私有 Bucket。
2. 必须关闭匿名访问和公开读权限。
3. 简历上传和访问必须通过签名 URL。
4. OSS 密钥、访问域名、Bucket 名称等敏感信息必须写入独立配置文件。
5. 禁止将敏感配置硬编码到业务代码中。

### 9.3 AI 调用

1. 仅轻量化使用 Eino 基础 Chat 对话组件。
2. AI 回答必须基于 MySQL 查询得到的真实业务上下文。
3. 禁止使用裸 HTTP 接口直连大模型替代 Eino。

## 10. 前端要求

### 10.1 HR 管理端

1. 使用独立前端工程与专属路由。
2. 包含登录、岗位管理、候选人台账、AI 对话窗口。
3. 前端只负责页面渲染、交互操作和接口请求。
4. 前端不得承载权限校验、文件处理、AI 逻辑或业务数据读写核心逻辑。

### 10.2 候选人用户端

1. 使用独立前端工程与专属路由。
2. 包含岗位浏览、注册登录、个人资料编辑、简历上传、岗位投递。
3. 前端只负责页面渲染、交互操作和接口请求。
4. 业务校验结果以后端为准。

## 11. 交付目录

所有代码与文档应放置在 `git根目录/final_homework/` 下：

| 路径 | 内容 |
| --- | --- |
| `final_homework/hr-frontend/` | HR 管理端前端全部源码 |
| `final_homework/user-frontend/` | 候选人用户端前端全部源码 |
| `final_homework/web-gin-service/` | Gin Web 网关服务工程代码 |
| `final_homework/logic-grpc-service/` | Logic gRPC 核心业务服务工程代码 |
| `final_homework/api.md` | 项目接口说明文档 |
| `final_homework/db.md` | 数据库设计文档 |
| `final_homework/README.md` | 启动部署、配置、服务启动顺序和项目亮点 |
| `final_homework/answer.md` | 三方 AI Agent 平台接入设计方案 |

## 12. 验收标准

### 12.1 功能验收

1. 游客可以免登录浏览公开岗位。
2. 候选人可以注册、登录、编辑档案、上传合规简历、投递岗位。
3. 候选人未完善档案或未上传合规简历时，系统能拦截投递并提示。
4. HR 可以注册、登录、新增、编辑、下架本人岗位。
5. HR 不能操作他人岗位。
6. HR 可以分页查看岗位投递候选人、档案和简历记录。
7. HR 可以通过 AI 对话查询真实业务统计数据。
8. AI 对话历史可持久化，刷新后可恢复上下文。

### 12.2 技术验收

1. Web 服务与 Logic 服务之间通过 gRPC 调用。
2. Web 服务不包含核心业务逻辑。
3. Logic 服务承载权限管控、岗位业务、OSS 签名、MySQL 读写和 Eino 封装。
4. 简历文件存入私有 OSS，不能存入本地目录。
5. OSS 访问使用签名 URL，不能依赖公开读。
6. 简历格式校验同时覆盖文件后缀与文件头。
7. AI 能力完整引用 Eino 框架依赖。

### 12.3 文档与提交验收

1. 提供 `api.md`，说明前后端接口。
2. 提供 `db.md`，说明数据表结构、字段和关联关系。
3. 提供 `README.md`，说明环境配置、启动顺序、OSS 密钥配置方式和项目亮点。
4. 提供 `answer.md`，回答 OpenClaw 或 Hermes 等三方 AI Agent 平台接入设计题。
5. 演示视频命名为 `姓名_学号_全栈大作业.mp4`。
6. 视频完整演示系统链路，并讲解两层 gRPC 调用逻辑和私有 OSS 签名 URL 安全上传下载。

## 13. 关键风险与注意事项

1. 权限校验不能只依赖前端展示控制，后端必须强制校验。
2. HR 岗位操作必须校验岗位归属，否则会出现越权管理风险。
3. 简历上传不能只校验文件后缀，还需要校验文件头。
4. OSS Bucket 如果开启公开读，会违反简历隐私安全要求。
5. AI 问答如果不查询 MySQL 真实业务数据，会偏离验收重点。
6. Web 服务如果直接写核心业务逻辑，会破坏两层 gRPC 架构要求。
7. 敏感配置不能硬编码到业务代码中。
